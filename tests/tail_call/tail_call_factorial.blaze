// Factorial tail call test
print("Factorial tail call test")

fn factorial(n: int, acc: int) -> int {
    if n <= 1 {
        return acc
    }
    return factorial(n - 1, n * acc)
}

print(factorial(5, 1))
print(factorial(10, 1))
